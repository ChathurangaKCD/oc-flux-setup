# Helm values for OpenChoreo Workflow Plane
# Adapted from upstream install/k3d/single-cluster/values-wp.yaml

argo-workflows:
  server:
    # Argo Workflows UI is enabled for testing/development purposes
    enabled: true
    serviceType: LoadBalancer
    servicePort: 10081
    authModes:
      - server
