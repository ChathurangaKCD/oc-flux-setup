---
# Self-signed ClusterIssuer for TLS certificates (used by cluster-gateway-ca and other internal certs)
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: openchoreo-selfsigned-issuer
spec:
  selfSigned: {}
