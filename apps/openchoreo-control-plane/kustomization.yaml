---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - cert-resources.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: openchoreo-control-plane-values
    namespace: openchoreo-control-plane
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
