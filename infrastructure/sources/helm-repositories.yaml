---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: jetstack
  namespace: flux-system
spec:
  interval: 1h
  url: https://charts.jetstack.io
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: openchoreo
  namespace: flux-system
spec:
  type: oci
  interval: 1h
  url: oci://ghcr.io/openchoreo/helm-charts
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: twuni
  namespace: flux-system
spec:
  interval: 1h
  url: https://twuni.github.io/docker-registry.helm
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: argo
  namespace: flux-system
spec:
  interval: 1h
  url: https://argoproj.github.io/argo-helm
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: external-secrets-oci
  namespace: flux-system
spec:
  type: oci
  interval: 1h
  url: oci://ghcr.io/external-secrets/charts
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: kgateway
  namespace: flux-system
spec:
  type: oci
  interval: 1h
  url: oci://cr.kgateway.dev/kgateway-dev/charts
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: asgardeo
  namespace: flux-system
spec:
  type: oci
  interval: 1h
  url: oci://ghcr.io/asgardeo/helm-charts
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: openbao
  namespace: flux-system
spec:
  type: oci
  interval: 1h
  url: oci://ghcr.io/openbao/charts
---
# GitRepository for openchoreo getting-started samples
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: openchoreo
  namespace: flux-system
spec:
  interval: 1h
  url: https://github.com/openchoreo/openchoreo
  ref:
    tag: v1.0.0
  # Whitelist pattern (gitignore semantics): exclude everything, then walk down
  # the tree re-including parent dirs and re-excluding unwanted siblings at each
  # level. This is required because !/samples/getting-started/ on its own would
  # recursively re-include EVERYTHING under getting-started, which causes
  # kustomize build to fail with "may not add resource with an already
  # registered id" (the per-resource files in ci-workflows/ etc. are already
  # merged into all.yaml).
  ignore: |
    /*
    !/samples
    /samples/*
    !/samples/getting-started
    /samples/getting-started/*
    !/samples/getting-started/all.yaml
    !/samples/getting-started/workflow-templates.yaml
    !/samples/getting-started/workflow-templates
